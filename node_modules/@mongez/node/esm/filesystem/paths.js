import { __spreadArray, __read } from './../__helpers/tslib/tslib.es6.js';
import path from "path";

/**
 * Get the absolute path to the root of the project for the given path
 */
function rootPath() {
  var morePaths = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    morePaths[_i] = arguments[_i];
  }
  return path.join.apply(
    path,
    __spreadArray([process.cwd()], __read(morePaths), false),
  );
}
/**
 * Get the absolute path to the src directory for the given path
 */
function srcPath() {
  var morePaths = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    morePaths[_i] = arguments[_i];
  }
  return rootPath.apply(
    void 0,
    __spreadArray(["src"], __read(morePaths), false),
  );
}

export { rootPath, srcPath };
