import {statSync,promises}from'fs';const cachedPaths = {};
/**
 * Get path stats
 */
function stats(path, cache = true) {
    if (!cache || !cachedPaths[path]) {
        cachedPaths[path] = statSync(path);
    }
    return cachedPaths[path];
}
/**
 * Get path stats async
 */
async function statsAsync(path, cache = true) {
    return new Promise(async (resolve, reject) => {
        try {
            const cachedPath = cachedPaths[path];
            if (cache && cachedPath) {
                resolve(cachedPath);
                return;
            }
            const stats = await promises.stat(path);
            cachedPaths[path] = stats;
            resolve(stats);
        }
        catch (error) {
            reject(error);
        }
    });
}export{stats,statsAsync};//# sourceMappingURL=stats.js.map
