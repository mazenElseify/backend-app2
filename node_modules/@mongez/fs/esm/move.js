import {renameSync,promises}from'fs';/**
 * Move the given source to new target
 */
function movePath(target, destination) {
    renameSync(target, destination);
}
const moveDirectory = movePath;
const moveFile = movePath;
/**
 * Move the given source to new target async
 */
async function movePathAsync(target, destination) {
    return promises.rename(target, destination);
}
const moveDirectoryAsync = movePathAsync;
const moveFileAsync = movePathAsync;export{moveDirectory,moveDirectoryAsync,moveFile,moveFileAsync,movePath,movePathAsync};//# sourceMappingURL=move.js.map
