import {writeFileSync,promises}from'fs';/**
 * Put the given object in the given path in json format
 */
function putJsonFile(path, data, options = { spaces: 4 }) {
    writeFileSync(path, JSON.stringify(data, options.replacer, options.spaces), "utf8");
}
/**
 * Put async the given object in the given path in json format
 */
async function putJsonFileAsync(path, data, options = { spaces: 4 }) {
    return new Promise(async (resolve, reject) => {
        try {
            await promises.writeFile(path, JSON.stringify(data, options.replacer, options.spaces), "utf8");
            resolve();
        }
        catch (error) {
            reject(error);
        }
    });
}export{putJsonFile,putJsonFileAsync};//# sourceMappingURL=putJsonFile.js.map
