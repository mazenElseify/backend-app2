import {renameSync,promises}from'fs';/**
 * Rename the given path to a new location
 */
function renamePath(target, destination) {
    renameSync(target, destination);
}
const renameFile = renamePath;
const renameDirectory = renamePath;
// async version
async function renamePathAsync(target, destination) {
    return promises.rename(target, destination);
}
const renameFileAsync = renamePathAsync;
const renameDirectoryAsync = renamePathAsync;export{renameDirectory,renameDirectoryAsync,renameFile,renameFileAsync,renamePath,renamePathAsync};//# sourceMappingURL=rename.js.map
