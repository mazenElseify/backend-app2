import {stats,statsAsync}from'./stats.js';/**
 * Get last modified time of the given path
 */
function lastModified(path) {
    try {
        return stats(path).mtime;
    }
    catch (e) {
        return new Date();
    }
}
const fileLastModified = lastModified;
const directoryLastModified = lastModified;
/**
 * Get last modified time of the given path async
 */
async function lastModifiedAsync(path) {
    return new Promise(async (resolve, reject) => {
        try {
            resolve((await statsAsync(path)).mtime);
        }
        catch (e) {
            reject(e);
        }
    });
}
const fileLastModifiedAsync = lastModifiedAsync;
const directoryLastModifiedAsync = lastModifiedAsync;export{directoryLastModified,directoryLastModifiedAsync,fileLastModified,fileLastModifiedAsync,lastModified,lastModifiedAsync};//# sourceMappingURL=lastModified.js.map
