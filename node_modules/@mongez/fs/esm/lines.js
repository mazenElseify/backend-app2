import {getFile,getFileAsync}from'./getFile.js';/**
 * Return the lines of the given file
 */
function fileLines(path) {
    try {
        return getFile(path).split(/\n|\r\n/);
    }
    catch (error) {
        throw error;
    }
}
/**
 * Get async the lines of the given file
 */
async function fileLinesAsync(path) {
    return new Promise(async (resolve, reject) => {
        try {
            const content = await getFileAsync(path);
            resolve(content.split(/\n|\r\n/));
        }
        catch (error) {
            reject(error);
        }
    });
}export{fileLines,fileLinesAsync};//# sourceMappingURL=lines.js.map
