import {existsSync,openSync,promises}from'fs';import {pathExistsAsync}from'./exists.js';/**
 * Touch the given file path
 */
function touch(location) {
    if (existsSync(location))
        return;
    openSync(location, "w");
}
/**
 * @async Touch the given file path
 */
async function touchAsync(path) {
    return new Promise(async (resolve) => {
        try {
            if (await pathExistsAsync(path))
                return resolve();
            promises.open(path, "w");
            resolve();
        }
        catch (error) {
            // Do nothing
        }
    });
}export{touch,touchAsync};//# sourceMappingURL=touch.js.map
