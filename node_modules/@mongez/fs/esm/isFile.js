import {stats,statsAsync}from'./stats.js';/**
 * Determine if the given path is directory
 */
function isFile(path) {
    try {
        return stats(path).isFile();
    }
    catch (e) {
        // lstatSync throws an error if path doesn't exist
        return false;
    }
}
/**
 * Check if the given path is file async
 */
async function isFileAsync(path) {
    return new Promise((resolve) => {
        statsAsync(path)
            .then((stat) => {
            resolve(stat.isFile());
        })
            .catch(() => {
            resolve(false);
        });
    });
}export{isFile,isFileAsync};//# sourceMappingURL=isFile.js.map
