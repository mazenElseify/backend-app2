import {accessSync,promises}from'fs';/**
 * Determine if the given path exists
 */
function pathExists(path) {
    try {
        accessSync(path);
        return true;
    }
    catch (error) {
        return false;
    }
}
const fileExists = pathExists;
const directoryExists = pathExists;
/**
 * @async Determine if the given path exists
 */
async function pathExistsAsync(path) {
    return new Promise(async (resolve) => {
        try {
            await promises.access(path);
            resolve(true);
        }
        catch (error) {
            resolve(false);
        }
    });
}
const fileExistsAsync = pathExistsAsync;
const directoryExistsAsync = pathExistsAsync;export{directoryExists,directoryExistsAsync,fileExists,fileExistsAsync,pathExists,pathExistsAsync};//# sourceMappingURL=exists.js.map
