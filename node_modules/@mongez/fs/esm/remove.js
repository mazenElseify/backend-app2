import {rmSync,promises}from'fs';/**
 * remove the given path
 */
function removePath(path, options = {
    force: true,
    recursive: true,
}) {
    rmSync(path, options);
}
const unlink = removePath;
const removeDirectory = removePath;
const rmdir = removePath;
/**
 * remove the given path async
 */
async function removePathAsync(path, options = {
    force: true,
    recursive: true,
}) {
    return promises.rm(path, options);
}
const unlinkAsync = removePathAsync;
const removeDirectoryAsync = removePathAsync;
const rmdirAsync = removePathAsync;export{removeDirectory,removeDirectoryAsync,removePath,removePathAsync,rmdir,rmdirAsync,unlink,unlinkAsync};//# sourceMappingURL=remove.js.map
