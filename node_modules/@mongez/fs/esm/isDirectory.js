import {stats,statsAsync}from'./stats.js';/**
 * Determine if the given path is directory
 */
function isDir(path) {
    try {
        return stats(path).isDirectory();
    }
    catch (e) {
        // lstatSync throws an error if path doesn't exist
        return false;
    }
}
const isDirectory = isDir;
/**
 * Determine if the given path is directory async
 */
async function isDirAsync(path) {
    try {
        return (await statsAsync(path)).isDirectory();
    }
    catch (e) {
        // lstatSync throws an error if path doesn't exist
        return false;
    }
}
const isDirectoryAsync = isDirAsync;export{isDir,isDirAsync,isDirectory,isDirectoryAsync};//# sourceMappingURL=isDirectory.js.map
