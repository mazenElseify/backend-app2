'use strict';var fs=require('fs');/**
 * Rename the given path to a new location
 */
function renamePath(target, destination) {
    fs.renameSync(target, destination);
}
const renameFile = renamePath;
const renameDirectory = renamePath;
// async version
async function renamePathAsync(target, destination) {
    return fs.promises.rename(target, destination);
}
const renameFileAsync = renamePathAsync;
const renameDirectoryAsync = renamePathAsync;exports.renameDirectory=renameDirectory;exports.renameDirectoryAsync=renameDirectoryAsync;exports.renameFile=renameFile;exports.renameFileAsync=renameFileAsync;exports.renamePath=renamePath;exports.renamePathAsync=renamePathAsync;//# sourceMappingURL=rename.js.map
