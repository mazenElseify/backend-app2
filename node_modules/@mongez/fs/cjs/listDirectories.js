'use strict';var isDirectory=require('./isDirectory.js'),list=require('./list.js');/**
 * Get all directories in the given path <Non Recursive>
 */
function listDirectories(path) {
    return list.list(path).filter((innerPath) => isDirectory.isDirectory(path + "/" + innerPath));
}
/**
 * Get all directories in the given path <Non Recursive> async
 */
async function listDirectoriesAsync(path) {
    return new Promise(async (resolve, reject) => {
        try {
            const files = await list.listAsync(path);
            resolve(files.filter((innerPath) => isDirectory.isDirectory(path + "/" + innerPath)));
        }
        catch (error) {
            reject(error);
        }
    });
}exports.listDirectories=listDirectories;exports.listDirectoriesAsync=listDirectoriesAsync;//# sourceMappingURL=listDirectories.js.map
