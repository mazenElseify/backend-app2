'use strict';var fs=require('fs');/**
 * Determine if the given path exists
 */
function pathExists(path) {
    try {
        fs.accessSync(path);
        return true;
    }
    catch (error) {
        return false;
    }
}
const fileExists = pathExists;
const directoryExists = pathExists;
/**
 * @async Determine if the given path exists
 */
async function pathExistsAsync(path) {
    return new Promise(async (resolve) => {
        try {
            await fs.promises.access(path);
            resolve(true);
        }
        catch (error) {
            resolve(false);
        }
    });
}
const fileExistsAsync = pathExistsAsync;
const directoryExistsAsync = pathExistsAsync;exports.directoryExists=directoryExists;exports.directoryExistsAsync=directoryExistsAsync;exports.fileExists=fileExists;exports.fileExistsAsync=fileExistsAsync;exports.pathExists=pathExists;exports.pathExistsAsync=pathExistsAsync;//# sourceMappingURL=exists.js.map
