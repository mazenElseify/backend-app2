'use strict';var fs=require('fs'),exists=require('./exists.js');/**
 * Touch the given file path
 */
function touch(location) {
    if (fs.existsSync(location))
        return;
    fs.openSync(location, "w");
}
/**
 * @async Touch the given file path
 */
async function touchAsync(path) {
    return new Promise(async (resolve) => {
        try {
            if (await exists.pathExistsAsync(path))
                return resolve();
            fs.promises.open(path, "w");
            resolve();
        }
        catch (error) {
            // Do nothing
        }
    });
}exports.touch=touch;exports.touchAsync=touchAsync;//# sourceMappingURL=touch.js.map
