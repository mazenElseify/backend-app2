'use strict';var fs=require('fs');/**
 * remove the given path
 */
function removePath(path, options = {
    force: true,
    recursive: true,
}) {
    fs.rmSync(path, options);
}
const unlink = removePath;
const removeDirectory = removePath;
const rmdir = removePath;
/**
 * remove the given path async
 */
async function removePathAsync(path, options = {
    force: true,
    recursive: true,
}) {
    return fs.promises.rm(path, options);
}
const unlinkAsync = removePathAsync;
const removeDirectoryAsync = removePathAsync;
const rmdirAsync = removePathAsync;exports.removeDirectory=removeDirectory;exports.removeDirectoryAsync=removeDirectoryAsync;exports.removePath=removePath;exports.removePathAsync=removePathAsync;exports.rmdir=rmdir;exports.rmdirAsync=rmdirAsync;exports.unlink=unlink;exports.unlinkAsync=unlinkAsync;//# sourceMappingURL=remove.js.map
