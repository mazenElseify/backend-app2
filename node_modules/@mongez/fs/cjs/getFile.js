'use strict';var fs=require('fs');/**
 * Get the content of the given file
 */
function getFile(path) {
    return fs.readFileSync(path, "utf8");
}
/**
 * @async Get the content of the given file
 */
async function getFileAsync(path) {
    return new Promise(async (resolve, reject) => {
        try {
            resolve(await fs.promises.readFile(path, "utf8"));
        }
        catch (error) {
            reject(error);
        }
    });
}exports.getFile=getFile;exports.getFileAsync=getFileAsync;//# sourceMappingURL=getFile.js.map
