'use strict';var fs=require('fs');/**
 * Put the given object in the given path in json format
 */
function putJsonFile(path, data, options = { spaces: 4 }) {
    fs.writeFileSync(path, JSON.stringify(data, options.replacer, options.spaces), "utf8");
}
/**
 * Put async the given object in the given path in json format
 */
async function putJsonFileAsync(path, data, options = { spaces: 4 }) {
    return new Promise(async (resolve, reject) => {
        try {
            await fs.promises.writeFile(path, JSON.stringify(data, options.replacer, options.spaces), "utf8");
            resolve();
        }
        catch (error) {
            reject(error);
        }
    });
}exports.putJsonFile=putJsonFile;exports.putJsonFileAsync=putJsonFileAsync;//# sourceMappingURL=putJsonFile.js.map
