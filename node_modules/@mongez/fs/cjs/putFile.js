'use strict';var fs=require('fs');/**
 * Put the given content into the given file path
 */
function putFile(path, content, options = "utf8") {
    fs.writeFileSync(path, String(content), options);
}
/**
 * @async Put the given content into the given file path
 */
async function putFileAsync(path, content, options = "utf8") {
    return new Promise(async (resolve, reject) => {
        try {
            await fs.promises.writeFile(path, String(content), options);
            resolve();
        }
        catch (error) {
            reject(error);
        }
    });
}exports.putFile=putFile;exports.putFileAsync=putFileAsync;//# sourceMappingURL=putFile.js.map
